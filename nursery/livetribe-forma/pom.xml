<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  
    <modelVersion>4.0.0</modelVersion>
    <groupId>org.livetribe.nursery</groupId>
    <artifactId>livetribe-forma</artifactId>
    <packaging>pom</packaging>
    <version>1.0-SNAPSHOT</version>
    <name>Livetribe :: Forma :: Rich Client Platform</name>

    <modules>
        <!-- The maven module must be the first, as it builds a maven plugin needed for the build -->
        <module>maven</module>
        <module>modules</module>
        <!-- The assemble module must be the last, as it assembles the artifacts generated by the build -->
        <module>assemble</module>
    </modules>

    <build>
      <defaultGoal>install</defaultGoal>
      <plugins>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-compiler-plugin</artifactId>
          <configuration>
            <source>1.5</source>
            <target>1.5</target>
          </configuration>
        </plugin>
      </plugins>
    </build>

    <repositories>
      <repository>
        <id>livetribe-repo</id>
        <name>Livetribe Repository</name>
        <url>http://repo.livetribe.org/maven2</url>
      </repository>
      <repository>
        <id>livetribe-snapshots</id>
        <name>Livetribe Repository</name>
        <url>http://repo.livetribe.org/maven2-snapshot</url>
      </repository>
    </repositories>
    
    <dependencies>
        <dependency>
            <groupId>junit</groupId>
            <artifactId>junit</artifactId>
            <version>3.8.1</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

</project>
